name: "[DISABLED] Deploy Flutter Web to GitHub Pages (gh-pages branch)"

on:
  workflow_dispatch:

jobs:
  build-and-deploy:
    if: ${{ false }} # Disabled; using official Pages workflow (pages.yml)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Flutter pub get
        run: flutter pub get

      - name: Build web
        run: flutter build web --release --base-href /christmas-bingo/

      - name: Add 404 fallback (SPA routing)
        run: cp build/web/index.html build/web/404.html

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
          publish_branch: gh-pages
          force_orphan: true
